const mocha = require('mocha')
const expect = require('chai').expect
const app = require('../index.js')

describe('App to make a todo list',()=>{
    
    it('Should see if express works',()=>{
        expect(app).to.exist
            
        })
       
    })